# YAML script to experiment with GH Workflows and Actions
name: GitHub Workflow and Actions Tests
run-name: ${{ github.actor }} is running this workflow

on: [push]

jobs:
  Try-GH-Workflows:
    runs-on: [ubuntu-latest, GPU]
    steps:
      - run: echo "Starting this workflow test!"
      - run: echo "This workflow is pushed in a new commit"
      - run: echo "And it is on an Ubuntu GPU Runner"
